name: Crystal CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image: crystaldata/numci

    steps:
    - uses: actions/checkout@v1
    - name: Dependencies
      run: shards install
    - name: Run tests
      run: crystal spec
